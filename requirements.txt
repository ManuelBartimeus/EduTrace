# EduTrace — reproduction environment for the locked run.
# Q4: every requirement is pinned with ==. Nothing here uses >=.
#
# This is the environment in which results/results.json and
# results/closeout_verdict.json were produced. It is sufficient to reproduce
# every number reported in the Methods and Results sections from the committed
# data artefacts.
#
# Regenerating the synthetic supplement is a separate, optional step with its
# own pinned environment: see requirements-synthesis.txt. It is not needed to
# reproduce any reported figure, because the supplement is committed.

numpy==2.4.4
pandas==3.0.2
scikit-learn==1.8.0
scipy==1.17.1
xgboost==3.2.0
imbalanced-learn==0.14.2
statsmodels==0.15.0
shap==0.51.0
torch==2.14.0
tab-transformer-pytorch==0.6.1
matplotlib==3.10.9
