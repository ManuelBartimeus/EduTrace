# EduTrace — OPTIONAL environment, for regenerating the synthetic supplement only.
#
# Install this in a SEPARATE virtual environment from requirements.txt. SDV pins
# pandas below 3.0, which is not the version the reported run was produced under,
# so the two environments are deliberately kept apart:
#
#   requirements.txt            -> reproduces every reported number (pandas 3.0.2)
#   requirements-synthesis.txt  -> regenerates data/synth_ctgan_s42.csv only
#
# Nothing in the Results section depends on this environment. The supplement is
# committed as a locked data artefact precisely so that reproducing the reported
# figures never requires re-running CTGAN.

sdv==1.38.3
numpy==2.4.4
pandas==2.3.3
scikit-learn==1.8.0
torch==2.14.0
